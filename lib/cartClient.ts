export async function ensureCartToken(): Promise<string> {
    /*
    * check local storage for cart token
    * if present return it
    * else request createGuestCart
    * write to local storage and return it
    * */
}

export async function addItemToCart(productId: number) {
    return productId
}

